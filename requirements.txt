
## Requirements
- pandas
- numpy
- scikit-learn

## How to Run
1. Clone the repository
2. Install the required libraries using `pip install -r requirements.txt`
3. Run the `src/churn_analysis.py` script
4. Check the results in `churn_predictions.csv`
